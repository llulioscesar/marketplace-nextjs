import type {Metadata} from "next";

export const metadata: Metadata = {
    title: "No autorizado",
};

export default function Page() {
    return (
        <>
            <h1>unauthorized</h1>
        </>
    )
}